import { AppPlaceholder } from "@/components/app-placeholder";

export default function LeadsPage() {
  return (
    <AppPlaceholder
      activePath="/app/leads"
      eyebrow="Pipeline"
      title="Every pack earns its next conversation."
      description="Capture pages, lead delivery, follow-up drafts, and CSV export will use this tab."
    />
  );
}
