import { AppPlaceholder } from "@/components/app-placeholder";

export default function CalendarPage() {
  return (
    <AppPlaceholder
      activePath="/app/calendar"
      eyebrow="Your plan"
      title="A calendar that compounds."
      description="Strategy and 30-day calendar generation build on this mobile-first home for your content plan."
    />
  );
}
